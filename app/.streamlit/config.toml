# CogniTrace tema yapılandırması (backlog: 'Dark/Light theme toggle')
# Streamlit açık/koyu geçişini yerleşik sunar: sağ üst ☰ → Settings → Theme.
# Buradaki ayarlar markanın rengini iki temada da uygular.
[theme]
base = "dark"
primaryColor = "#6C63FF"
